# Plaintext tracker data — only the encrypted file is committed
data/data.json

# Built distributable (regenerated daily; lives in the Teams channel, not git)
dist/

# Secrets (team passphrase)
.secrets/

# Never commit meeting transcripts or the Excel tracker
*.docx
*.xlsx
*.xlsm

# OS noise
Thumbs.db
.DS_Store
.claude/settings.local.json
