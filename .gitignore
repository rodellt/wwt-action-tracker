# Tracker data never goes to GitHub (v3: the repo is code-only — data lives in
# the SharePoint file's embedded envelope; these are local working files)
data/

# Built distributable (regenerated daily; lives in the Teams channel, not git)
dist/

# Secrets (team passphrase)
.secrets/

# Never commit meeting transcripts or the Excel tracker
*.docx
*.xlsx
*.xlsm

# OS noise
Thumbs.db
.DS_Store
.claude/settings.local.json
